# no-array-index-key

```jsx
const studies = [{ name: 'hello' }, { name: 'world' }];

const Test = () => {
  return (
    <ul>
      {studies.map((study, index) => (
        <li key={index}>{study.name}</li>
      ))}
    </ul>
  );
};
```
